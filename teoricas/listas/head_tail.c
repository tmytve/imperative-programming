//funcion para acceder mas facil desde el main al head de la lista
int head(tList l){
    return l->head;
}

//funcion para acceder mas facil desde el main al tail de la lista
int tail(tList l){
    return l->tail;
}